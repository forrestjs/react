export { useMUITheme } from "./use-mui-theme";
export default reactMUI;
/**
 * @param {ForrestJSContext} ctx
 * @returns {Array.<ForrestJSExtension>}
 */
export function reactMUI({ registerTargets, registerAction, createExtension, getConfig, }: ForrestJSContext): Array<ForrestJSExtension>;
