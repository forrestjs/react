{
  "name": "@ForrestJS/react-libraries",
  "scripts": {
    "boot": "npm install && lerna bootstrap",
    "clean": "lerna run clean && lerna clean --yes && rm -rf node_modules",
    "build": "lerna run build",
    "deploy": "lerna run build && lerna publish",
    "force-deploy": "lerna run build && lerna publish --force-publish"
  },
  "dependencies": {
    "@babel/cli": "^7.15.7",
    "@babel/preset-env": "^7.15.8",
    "@babel/preset-react": "^7.14.5",
    "cross-env": "^7.0.3",
    "lerna": "^4.0.0",
    "react": "^17.0.2",
    "react-dom": "^17.0.2",
    "react-scripts": "4.0.3",
    "web-vitals": "^2.1.2"
  }
}
