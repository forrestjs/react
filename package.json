{
  "name": "@forrestjs/react-libraries",
  "scripts": {
    "boot": "npm install && lerna bootstrap",
    "clean": "lerna run clean && lerna clean --yes && rm -rf node_modules",
    "build": "lerna run build",
    "deploy": "lerna run build && lerna publish",
    "deploy:all": "lerna run build && lerna publish --force-publish"
  },
  "devDependencies": {
    "@babel/cli": "^7.16.8",
    "@babel/preset-env": "^7.16.11",
    "@babel/preset-react": "^7.16.7",
    "cross-env": "^7.0.3",
    "lerna": "^4.0.0",
    "react-scripts": "5.0.0"
  }
}
